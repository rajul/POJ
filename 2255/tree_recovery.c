#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
	char preorder_repr[30];
	char inorder_repr[30];

	while(scanf("%s %s\n", preorder_repr, inorder_repr) != EOF)
	{
		
	}

	return 0;
}